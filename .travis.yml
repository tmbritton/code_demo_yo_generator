language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-code-demo' ]; then cd .. && eval "mv $currentfolder generator-code-demo" && cd generator-code-demo; fi

